---
import { Image } from 'astro:assets';
import {
    BASESCAN_TOKEN_URL,
    GECKOTERMINAL_POOL_URL,
    UNISWAP_BUY_URL,
} from '../web3/constants';

interface Props {
    locale?: 'en' | 'pt-BR';
}
const { locale = 'pt-BR' } = Astro.props;
const pt = locale === 'pt-BR';
const basePartnerLabel = pt ? 'Parceiro de Infraestrutura' : 'Infrastructure Partner';
const basePartnerTitle = pt ? 'Suportado pela Base' : 'Powered by Base';
const basePartnerBody = pt
    ? 'Liquidação em Base Mainnet com infraestrutura L2 robusta para escalar com segurança.'
    : 'Settlement on Base Mainnet with robust L2 infrastructure built for secure scaling.';
---
<footer class="footer-area">
    <div class="wrap">
        <div class="footer-box">
            <div class="bc-label" style="text-align: center; margin-bottom: 0.75rem;">{pt ? 'Links do Ecossistema' : 'Ecosystem Links'}</div>
            <div class="bc-title" style="text-align: center; margin-bottom: 2rem;">{pt ? 'Entre na rede' : 'Join the network'}</div>
            <div class="links-cluster-footer">
                <a href="https://neoflowoff.agency" class="link-tile-v2" target="_blank" rel="noopener">
                    <span class="link-tile-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"></circle><line x1="2" y1="12" x2="22" y2="12"></line><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"></path></svg>
                    </span> {pt ? 'Site' : 'Website'}</a>
                <a href={UNISWAP_BUY_URL} class="link-tile-v2" style="border-color:var(--green);color:var(--green);" target="_blank" rel="noopener">
                    <span class="link-tile-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="var(--green)" stroke-width="2.5"><path d="M7 17L17 7M17 7H7M17 7V17"/></svg>
                    </span> {pt ? 'Swap on Uniswap' : 'Swap on Uniswap'}</a>
                <a href={BASESCAN_TOKEN_URL} class="link-tile-v2" target="_blank" rel="noopener">
                    <span class="link-tile-icon">
                        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M21 16.5V7.5L12 2.25L3 7.5V16.5L12 21.75L21 16.5ZM12 4.125L18.75 8.25V15.75L12 19.875L5.25 15.75V8.25L12 4.125Z"/></svg>
                    </span>
                    Basescan</a>
                <a href="https://x.com/neoflw_on_chain" class="link-tile-v2" target="_blank" rel="noopener">
                    <span class="link-tile-icon">
                        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M18.901 1.153h3.68l-8.04 9.19L24 22.846h-7.406l-5.8-7.584-6.638 7.584H.474l8.6-9.83L0 1.154h7.594l5.243 6.932 6.064-6.932zm-1.292 19.49h2.039L6.486 3.24H4.298l13.311 17.403z"/></svg>
                    </span> X</a>
                <a href={GECKOTERMINAL_POOL_URL} class="link-tile-v2" target="_blank" rel="noopener">
                    <span class="link-tile-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20V10M18 20V4M6 20v-4"/></svg>
                    </span>
                    GeckoTerminal</a>
            </div>

            <div class="footer-base-powered-text" aria-label={basePartnerTitle}>
                <span class="base-powered-kicker">{basePartnerLabel}</span>
                <span class="base-powered-title">{basePartnerTitle}</span>
                <span class="base-powered-body">{basePartnerBody}</span>
                <a
                    class="base-powered-logo-link"
                    href="https://base.org"
                    target="_blank"
                    rel="noopener noreferrer"
                    aria-label={pt ? 'Abrir site da Base' : 'Open Base website'}
                >
                    <Image
                        class="base-powered-logo"
                        src="/Base_lockup_white.webp"
                        alt="Base"
                        width="300"
                        height="77"
                        loading="lazy"
                    />
                </a>
            </div>

            <div class="footer-note" style="margin-top: 2.5rem; text-align: center; color: var(--text-3); font-size: 0.7rem; border-top: 1px solid var(--border); padding-top: 2rem;">
                <div style="font-weight: 900; font-size: 1rem; letter-spacing: -0.02em; margin-bottom: 0.5rem;"><span style="color: var(--pink);">NEØ</span>FLW</div>
                <div style="margin-bottom: 0.25rem;">neoflowoff.eth · neoflowoff.agency · NEØ Flowoff Marketing e Assessoria Digital LTDA</div>
                <div>&copy; 2026 NEØ Protocol. {pt ? 'Todos os direitos reservados.' : 'All rights reserved.'}</div>
                <div style="margin-top: 0.5rem; font-style: italic;">Code is law. Expand until chaos becomes protocol.</div>
            </div>
        </div>
    </div>
</footer>

<style>
    .footer-base-powered-text {
        margin-top: 2.5rem;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        gap: 0.35rem;
        text-align: left;
    }

    .footer-base-powered-text .base-powered-kicker {
        font-size: 0.6rem;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        color: var(--blue);
        font-weight: 800;
        opacity: 0.75;
    }

    .footer-base-powered-text .base-powered-title {
        font-size: 1rem;
        font-weight: 700;
        color: var(--text);
    }

    .footer-base-powered-text .base-powered-body {
        font-size: 0.78rem;
        color: var(--text-2);
        line-height: 1.6;
    }

    .base-powered-logo-link {
        margin-top: 0.6rem;
        display: inline-flex;
        opacity: 0.9;
        transition: opacity 0.2s ease;
    }

    .base-powered-logo-link:hover {
        opacity: 1;
    }

    .base-powered-logo {
        height: auto;
        width: min(220px, 100%);
    }
</style>
